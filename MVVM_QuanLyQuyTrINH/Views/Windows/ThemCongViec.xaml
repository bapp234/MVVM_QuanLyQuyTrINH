<Window x:Class="MVVM_QuanLyQuyTrINH.Views.Windows.ThemCongViec"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thêm Công Việc Mới" Height="700" Width="550" 
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent">

    <Window.Resources>
        <Style x:Key="ModernComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="Padding" Value="10,0,30,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="30"/>
                            </Grid.ColumnDefinitions>

                            <Border x:Name="Border" Grid.ColumnSpan="2" 
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6">
                            </Border>

                            <ContentPresenter x:Name="ContentSite"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="10,0,0,0"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"/>

                            <ToggleButton Grid.Column="0" Grid.ColumnSpan="2" 
                                          Focusable="false"
                                          IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="Transparent"/>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <!-- Icon mũi tên nhỏ gọn -->
                            <Path Grid.Column="1" Data="M0,0 L4,4 L8,0" Stroke="#888" StrokeThickness="1.5" 
                                  HorizontalAlignment="Center" VerticalAlignment="Center" IsHitTestVisible="False"/>

                            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" 
                                   AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="White" BorderThickness="1" BorderBrush="#E0E0E0" CornerRadius="6" Margin="0,2,0,0">
                                        <Border.Effect>
                                            <DropShadowEffect Color="#B0B0B0" BlurRadius="10" ShadowDepth="2" Opacity="0.2"/>
                                        </Border.Effect>
                                        <ScrollViewer Margin="2" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#3AA0AD"/>
                                <Setter TargetName="Border" Property="Background" Value="#F9FAFB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 2. STYLE TEXTBOX ĐẸP -->
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" CornerRadius="6" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#3AA0AD"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#3AA0AD"/>
                                <Setter TargetName="border" Property="Background" Value="#F0F8FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 3. STYLE DATEPICKER ĐẸP -->
        <Style TargetType="DatePicker">
            <Setter Property="Height" Value="35"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

    </Window.Resources>

    <!-- KHUNG CHÍNH -->
    <Border CornerRadius="12" BorderBrush="#3AA0AD" BorderThickness="1" Background="White">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" Opacity="0.3" ShadowDepth="5"/>
        </Border.Effect>

        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Header -->
                <RowDefinition Height="*"/>
                <!-- Form -->
                <RowDefinition Height="Auto"/>
                <!-- Buttons -->
            </Grid.RowDefinitions>

            <!-- HEADER -->
            <TextBlock Text="📝 Thêm Công Việc Mới" FontSize="22" FontWeight="Bold" Foreground="#3AA0AD" 
                       HorizontalAlignment="Center" Margin="0,0,0,25"/>

            <!-- FORM NHẬP LIỆU -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,10,0">
                <StackPanel>

                    <!-- Tên công việc -->
                    <TextBlock Text="Tên công việc" FontWeight="SemiBold" Foreground="#555" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtTenCv" Margin="0,0,0,15"/>

                    <!-- Mô tả -->
                    <TextBlock Text="Mô tả chi tiết" FontWeight="SemiBold" Foreground="#555" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtMoTa" Height="80" TextWrapping="Wrap" AcceptsReturn="True" 
                             VerticalContentAlignment="Top" Padding="10" Margin="0,0,0,15"/>

                    <!-- Ngày tháng (2 cột) -->
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Ngày bắt đầu" FontWeight="SemiBold" Foreground="#555" Margin="0,0,0,5"/>
                            <DatePicker x:Name="dpNgayGiao"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Hạn hoàn thành" FontWeight="SemiBold" Foreground="#555" Margin="0,0,0,5"/>
                            <DatePicker x:Name="dpHanHoanThanh"/>
                        </StackPanel>
                    </Grid>

                    <!-- Dự án & Bước làm -->
                    <TextBlock Text="Thuộc Dự án" FontWeight="SemiBold" Foreground="#555" Margin="0,0,0,5"/>
                    <ComboBox x:Name="cbDuAn" Style="{StaticResource ModernComboBoxStyle}" Margin="0,0,0,15"
                              DisplayMemberPath="TenDuAn" SelectedValuePath="MaDuAn"/>

                    <TextBlock Text="Bước thực hiện (Quy trình)" FontWeight="SemiBold" Foreground="#555" Margin="0,0,0,5"/>
                    <ComboBox x:Name="cbBuocLam" Style="{StaticResource ModernComboBoxStyle}" Margin="0,0,0,15"
                              DisplayMemberPath="TenBuoc" SelectedValuePath="MaBuoc" IsEnabled="False"/>

                    <!-- Nhân viên tham gia -->
                    <TextBlock Text="Nhân viên tham gia"
           FontWeight="SemiBold" Foreground="#555" Margin="0,0,0,5"/>

                    <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="6" Background="White" Padding="8">
                        <ScrollViewer Height="160">
                            <ItemsControl x:Name="icNhanVien">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="0,2" Padding="5" CornerRadius="4" Background="White">
                                            <Border.Effect>
                                                <DropShadowEffect BlurRadius="4" ShadowDepth="0" Color="#00000020" Opacity="0.1"/>
                                            </Border.Effect>

                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                <!-- Checkbox -->
                                                <CheckBox Content="{Binding HoTen}"
                                      Tag="{Binding MaNv}"
                                      Margin="5,0"
                                      VerticalAlignment="Center"
                                      FontSize="14"
                                      Cursor="Hand"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Border>


                    <!-- Độ ưu tiên & Trạng thái (Ẩn) -->
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Độ ưu tiên -->
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Độ ưu tiên" FontWeight="SemiBold" Foreground="#555" Margin="0,0,0,5"/>
                            <ComboBox x:Name="cbDoUuTien" Style="{StaticResource ModernComboBoxStyle}">
                                <ComboBoxItem Content="Thấp"/>
                                <ComboBoxItem Content="Trung bình"/>
                                <ComboBoxItem Content="Cao"/>
                                <ComboBoxItem Content="Khẩn cấp"/>
                            </ComboBox>
                        </StackPanel>

                        <!-- Trạng thái (ẨN ĐI VÌ MẶC ĐỊNH LÀ MỚI TẠO) -->
                        <StackPanel Grid.Column="2" Visibility="Collapsed">
                            <TextBlock Text="Trạng thái" FontWeight="SemiBold" Foreground="#555" Margin="0,0,0,5"/>
                            <ComboBox x:Name="cbTrangThai" Style="{StaticResource ModernComboBoxStyle}">
                                <ComboBoxItem Content="Mới tạo"/>
                            </ComboBox>
                        </StackPanel>
                    </Grid>

                </StackPanel>
            </ScrollViewer>

            <!-- BUTTONS -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Hủy bỏ" Width="100" Height="40" Background="#F5F5F5" Foreground="#333" 
                        BorderThickness="0" Margin="0,0,15,0" Cursor="Hand" Click="btnCancel_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <Button Content="Lưu Công Việc" Width="140" Height="40" Background="#3AA0AD" Foreground="White" 
                        FontWeight="Bold" BorderThickness="0" Cursor="Hand" Click="btnSave_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                    <Button.Effect>
                        <DropShadowEffect Color="#3AA0AD" BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
                    </Button.Effect>
                </Button>
            </StackPanel>

        </Grid>
    </Border>
</Window>