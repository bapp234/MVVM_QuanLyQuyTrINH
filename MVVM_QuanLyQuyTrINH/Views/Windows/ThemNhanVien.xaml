<Window x:Class="MVVM_QuanLyQuyTrINH.Views.Windows.ThemNhanVien"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thêm Nhân Viên Mới" Height="550" Width="500" WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    <Border CornerRadius="10" BorderBrush="#3AA0AD" BorderThickness="2" Padding="20" Background="White">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="👤 Thêm Nhân Viên Mới"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="#2C8A8A"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20" />

            <StackPanel Grid.Row="1" Margin="0,10,0,10">
                <TextBlock Text="Tên đăng nhập:" FontWeight="SemiBold"/>
                <TextBox x:Name="txtTenDangNhap" Height="30" Margin="0,5,0,10"/>

                <TextBlock Text="Mật khẩu:" FontWeight="SemiBold"/>
                <PasswordBox x:Name="txtMatKhau" Height="30" Margin="0,5,0,10"/>

                <TextBlock Text="Họ và tên:" FontWeight="SemiBold"/>
                <TextBox x:Name="txtHoTen" Height="30" Margin="0,5,0,10"/>

                <TextBlock Text="Email:" FontWeight="SemiBold"/>
                <TextBox x:Name="txtEmail" Height="30" Margin="0,5,0,10"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="💾 Lưu"
                        Width="100"
                        Height="35"
                        Background="SeaGreen"
                        Foreground="White"
                        FontWeight="Bold"
                        Margin="0,0,10,0"
                        Click="btnSave_Click"/>
                <Button Content="✖ Hủy"
                        Width="100"
                        Height="35"
                        Background="#E53935"
                        Foreground="White"
                        FontWeight="Bold"
                        Click="btnCancel_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
