<Page x:Class="MVVM_QuanLyQuyTrINH.Views.Pages.QuanLyQuyTrinh"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1100"
      Title="Quản Lý Quy Trình">

    <Page.Resources>
        <!-- Style Nút tròn nhỏ (Giữ nguyên) -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="16" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Background="#F4F7F9">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 1. HEADER -->
        <Grid Grid.Row="0" Margin="30,20,30,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Width="40" Height="40" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" Cursor="Hand" Click="btnBack_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="20" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1">
                            <Path Data="M20,11H7.83L13.42,5.41L12,4L4,12L12,20L13.41,18.59L7.83,13H20V11Z" Fill="#3AA0AD" Stretch="Uniform" Width="16" Height="16"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <StackPanel Grid.Column="1" Margin="20,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="Quản Lý Quy Trình" FontSize="24" FontWeight="Bold" Foreground="#3AA0AD"/>
                <TextBlock Text="Thiết lập và theo dõi các quy trình làm việc." FontSize="14" Foreground="Gray"/>
            </StackPanel>
        </Grid>

        <!-- 2. TOOLBAR -->
        <Grid Grid.Row="1" Margin="30,0,30,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Nút Thêm Mới -->
            <Button Grid.Column="1" Content="+ Thêm Quy Trình" Width="160" Height="45" 
                    Background="#3AA0AD" Foreground="White" FontWeight="Bold" 
                    BorderThickness="0" Cursor="Hand" Click="btnAddProcess_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>

        <!-- 3. DANH SÁCH QUY TRÌNH -->
        <ScrollViewer Grid.Row="2" Padding="20,0" VerticalScrollBarVisibility="Auto" BorderThickness="0">
            <ItemsControl x:Name="ProcessList">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Left"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <!-- THẺ QUY TRÌNH -->
                        <Border Width="320" Height="180" Margin="15" Background="White" CornerRadius="10">
                            <Border.Effect>
                                <DropShadowEffect Color="#B0B0B0" BlurRadius="15" ShadowDepth="3" Opacity="0.15"/>
                            </Border.Effect>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="6"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Dải màu trạng thái -->
                                <Border Grid.Column="0" Background="{Binding MauTrangThai}" CornerRadius="10,0,0,10"/>

                                <!-- Nội dung -->
                                <Grid Grid.Column="1" Margin="15,12,15,12">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <!-- Tên -->
                                        <RowDefinition Height="Auto"/>
                                        <!-- Mã -->
                                        <RowDefinition Height="*"/>
                                        <!-- Mô tả -->
                                        <RowDefinition Height="Auto"/>
                                        <!-- Footer -->
                                    </Grid.RowDefinitions>

                                    <!-- Tên Quy Trình -->
                                    <TextBlock Text="{Binding TenDuAn}" Grid.Row="0" FontSize="16" FontWeight="Bold" Foreground="#333" TextTrimming="CharacterEllipsis"/>

                                    <!-- Mã Quy Trình -->
                                    <TextBlock Text="{Binding MaHienThi}" Grid.Row="1" FontSize="11" FontWeight="SemiBold" Foreground="#999" Margin="0,2,0,5"/>

                                    <!-- Thống kê số liệu (Steps & Tasks) -->
                                    <StackPanel Grid.Row="2" Orientation="Horizontal" VerticalAlignment="Top">
                                        <Border Background="#E3F2FD" CornerRadius="5" Padding="8,4" Margin="0,0,10,0">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="👣 "/>
                                                <TextBlock Text="{Binding SoLuongBuoc, StringFormat='{}{0} Bước'}" FontWeight="Bold" Foreground="#1565C0"/>
                                            </StackPanel>
                                        </Border>
                                        <Border Background="#E8F5E9" CornerRadius="5" Padding="8,4">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="📋 "/>
                                                <TextBlock Text="{Binding SoLuongCongViec, StringFormat='{}{0} Việc'}" FontWeight="Bold" Foreground="#2E7D32"/>
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>

                                    <!-- Footer: Trưởng nhóm -->
                                    <StackPanel Grid.Row="3" Orientation="Horizontal" VerticalAlignment="Bottom">
                                        <TextBlock Text="👑 Trưởng nhóm: " FontSize="12" Foreground="Gray"/>
                                        <TextBlock Text="{Binding TenTruongNhom}" FontSize="12" FontWeight="Bold" Foreground="#333"/>
                                    </StackPanel>
                                </Grid>

                                <!-- Overlay Thao tác -->
                                <Border x:Name="ActionPanel" Grid.ColumnSpan="2" Background="White" CornerRadius="10" Opacity="0" Visibility="Collapsed">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal">
                                        <Button Style="{StaticResource ActionButtonStyle}" Background="#29B6F6" ToolTip="Chi tiết" Click="ViewProcess_Click">
                                            <TextBlock Text="👁" Foreground="White"/>
                                        </Button>
                                        <Button Style="{StaticResource ActionButtonStyle}" Background="#EF5350" ToolTip="Xóa" Click="DeleteProcess_Click">
                                            <TextBlock Text="🗑" Foreground="White"/>
                                        </Button>
                                    </StackPanel>
                                </Border>

                                <!-- Triggers Hover (Giữ nguyên như QuanLyCongViec) -->
                                <!-- Bắp copy đoạn Border.Triggers từ QuanLyCongViec sang đây nhé -->
                                <Border.Triggers>
                                    <EventTrigger RoutedEvent="MouseEnter">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ActionPanel" Storyboard.TargetProperty="Visibility">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{x:Static Visibility.Visible}"/>
                                                </ObjectAnimationUsingKeyFrames>
                                                <DoubleAnimation Storyboard.TargetName="ActionPanel" Storyboard.TargetProperty="Opacity" To="0.98" Duration="0:0:0.2"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                    <EventTrigger RoutedEvent="MouseLeave">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="ActionPanel" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2"/>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ActionPanel" Storyboard.TargetProperty="Visibility">
                                                    <DiscreteObjectKeyFrame KeyTime="0:0:0.2" Value="{x:Static Visibility.Collapsed}"/>
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Border.Triggers>

                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>