<Page x:Class="MVVM_QuanLyQuyTrINH.Views.Pages.BaoCao"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="Báo cáo"
      Background="#F5F7FA"
      FontSize="16">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="30" Orientation="Vertical">

            <!-- TỔNG QUAN -->
            <Border Background="White" CornerRadius="12" Padding="20" BorderBrush="#3AA0AD" BorderThickness="1.5" Margin="0,0,0,25">
                <StackPanel>
                    <TextBlock Text="TỔNG QUAN" FontSize="22" FontWeight="Bold" Foreground="#3AA0AD" Margin="0,0,0,15"/>

                    <!-- Hàng 1 -->
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Tổng số dự án:" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding TongSoDuAn}" Margin="8,0,0,0"/>

                        <TextBlock Text="Tổng số nhân viên:" FontWeight="SemiBold" Margin="30,0,0,0"/>
                        <TextBlock Text="{Binding TongSoNhanVien}" Margin="8,0,0,0"/>

                        <TextBlock Text="Tổng công việc:" FontWeight="SemiBold" Margin="30,0,0,0"/>
                        <TextBlock Text="{Binding TongCv}" Margin="8,0,0,0"/>
                    </StackPanel>

                    <!-- Hàng 2 -->
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <TextBlock Text="Hoàn thành:" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding TongCvHoanThanh}" Margin="8,0,0,0"/>

                        <TextBlock Text="Đang làm:" FontWeight="SemiBold" Margin="30,0,0,0"/>
                        <TextBlock Text="{Binding TongCvDangLam}" Margin="8,0,0,0"/>

                        <TextBlock Text="Trễ hạn:" FontWeight="SemiBold" Margin="30,0,0,0"/>
                        <TextBlock Text="{Binding TongCvTreHan}" Margin="8,0,0,0"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <ItemsControl ItemsSource="{Binding BaoCaoDuAns}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" CornerRadius="12" Padding="20"
                    BorderBrush="#3AA0AD" BorderThickness="1.5" Margin="0,0,0,20">

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="220"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- THÊM 8 ROWS ĐỂ KHÔNG BỊ CHỒNG -->
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Dòng 1 -->
                                <TextBlock Text="Tên dự án:" Grid.Row="0" Grid.Column="0" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                <TextBlock Text="{Binding TenDuAn}" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8"/>

                                <!-- Dòng 2 -->
                                <TextBlock Text="Tổng công việc:" Grid.Row="1" Grid.Column="0" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                <TextBlock Text="{Binding TongCv}" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8"/>

                                <!-- Dòng 3 -->
                                <TextBlock Text="Công việc hoàn thành:" Grid.Row="2" Grid.Column="0" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                <TextBlock Text="{Binding CvhoanThanh}" Grid.Row="2" Grid.Column="1" Foreground="#48BB78" Margin="0,0,0,8"/>

                                <!-- Dòng 4 -->
                                <TextBlock Text="Công việc đang làm:" Grid.Row="3" Grid.Column="0" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                <TextBlock Text="{Binding CvdangLam}" Grid.Row="3" Grid.Column="1" Foreground="#4299E1" Margin="0,0,0,8"/>

                                <!-- Dòng 5 -->
                                <TextBlock Text="Công việc trễ hạn:" Grid.Row="4" Grid.Column="0" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                <TextBlock Text="{Binding CvtreHan}" Grid.Row="4" Grid.Column="1" Foreground="#F56565" Margin="0,0,0,8"/>

                                <!-- Dòng 6 -->
                                <TextBlock Text="Tỷ lệ hoàn thành:" Grid.Row="5" Grid.Column="0" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                <TextBlock Text="{Binding TyLeHoanThanh, StringFormat={}{0:P2}}" 
                               Grid.Row="5" Grid.Column="1" Foreground="#3AA0AD" Margin="0,0,0,8"/>

                                <!-- Dòng 7 -->
                                <TextBlock Text="Ngày báo cáo:" Grid.Row="6" Grid.Column="0" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                <TextBlock Text="{Binding NgayBaoCao, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}"
                               Grid.Row="6" Grid.Column="1" Margin="0,0,0,8"/>

                                <!-- Dòng 8 -->
                                <TextBlock Text="Tiến độ:" Grid.Row="7" Grid.Column="0" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                <ProgressBar Grid.Row="7" Grid.Column="1"
                                 Value="{Binding TyLeHoanThanh}"
                                 Maximum="1" Height="16" Margin="0,4,0,0"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </StackPanel>
    </ScrollViewer>
</Page>
