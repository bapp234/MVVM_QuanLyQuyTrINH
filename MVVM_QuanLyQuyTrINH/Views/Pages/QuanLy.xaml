<Page x:Class="MVVM_QuanLyQuyTrINH.Views.Pages.QuanLy"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="Quản Lý">

    <Page.Resources>
        <Style x:Key="DashboardCardStyle" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="15"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="20" 
                                BorderThickness="0">
                            <Border.Effect>
                                <DropShadowEffect Color="#B0B0B0" Direction="270" ShadowDepth="3" BlurRadius="10" Opacity="0.3"/>
                            </Border.Effect>

                            <Grid Margin="20">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E0F7FA"/>
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1.02" ScaleY="1.02" CenterX="0.5" CenterY="0.5"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#B2EBF2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CardTitleText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#3AA0AD"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,15,0,5"/>
        </Style>

        <Style x:Key="CardDescText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="Gray"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
    </Page.Resources>

    <Grid Background="#F9F9F9">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ===== TIÊU ĐỀ ===== -->
        <TextBlock Text="Quản Lý"
                   Foreground="#3AA0AD"
                   FontSize="26"
                   FontWeight="Bold"
                   Margin="30,30,0,20"/>

        <!-- ===== MENU BUTTON GROUP ===== -->
        <Grid Grid.Row="1" Margin="0,20,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- === Hàng 1: 3 nút === -->
            <Button Content="Quản Lý Quy Trình"
            Style="{StaticResource NavigationButtonStyle}"
            Grid.Row="0" Grid.Column="0"
            Margin="10"
            Click="btnQuanLyQuyTrinh_Click"/>

            <Button Content="Quản Lý Công Việc"
            Style="{StaticResource NavigationButtonStyle}"
            Grid.Row="0" Grid.Column="1"
            Margin="10"
            Click="btnQuanLyCongViec_Click"/>

            <Button Content="Quản Lý Nhân Viên"
            Style="{StaticResource NavigationButtonStyle}"
            Grid.Row="1" Grid.Column="0"
            Margin="10"
            Click="btnQuanLyNhanVien_Click"/>

            <Button Content="Phân Quyền"
            Style="{StaticResource NavigationButtonStyle}"
            Grid.Row="1" Grid.Column="1"
            Margin="10"
            Click="btnPhanQuyen_Click"/>

        </Grid>
    </Grid>
</Page>