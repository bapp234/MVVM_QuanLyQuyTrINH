<Page x:Class="MVVM_QuanLyQuyTrINH.Views.Pages.PhongBan"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="600"
      d:DesignWidth="1000"
      Title="Quản Lý Phòng Ban">

    <Page.Resources>
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="16" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Background="#F4F7F9" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="23*"/>
            </Grid.ColumnDefinitions>

            <Button Width="40" Height="40"
                    Background="White"
                    BorderBrush="#E0E0E0"
                    BorderThickness="1"
                    Cursor="Hand" VerticalAlignment="Top"
                    Click="btnBack_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="20" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1">
                            <Path Data="M20,11H7.83L13.42,5.41L12,4L4,12L12,20L13.41,18.59L7.83,13H20V11Z" 
                                  Fill="#3AA0AD" Stretch="Uniform" Width="16" Height="16"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <StackPanel Grid.Column="1" Margin="20,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="Quản Lý Phòng Ban"
                           Foreground="#3AA0AD"
                           FontSize="24"
                           FontWeight="Bold"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Height="45" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="10">
                <Border.Effect>
                    <DropShadowEffect Color="#E0E0E0" BlurRadius="10" ShadowDepth="2" Opacity="0.5"/>
                </Border.Effect>
                <Grid VerticalAlignment="Center" Margin="15,0">
                    <TextBlock Text="🔍" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                    <TextBox x:Name="SearchTextBox"
                             FontSize="14"
                             BorderThickness="0"
                             Background="Transparent"
                             Padding="30,0,0,0"
                             VerticalContentAlignment="Center"
                             Height="45"
                             />
                    <TextBlock x:Name="PlaceholderText"
                               Text="Tìm kiếm theo tên, mã phòng..."
                               Foreground="#AAA"
                               FontSize="14"
                               VerticalAlignment="Center"
                               Margin="30,0,0,0"
                               IsHitTestVisible="False"/>
                </Grid>
            </Border>

            <Button Grid.Column="2"
                    Content="+ Thêm Phòng Ban"
                    Width="160"
                    Background="#3AA0AD"
                    Foreground="White"
                    FontWeight="SemiBold"
                    BorderThickness="0"
                    Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
                <Button.Effect>
                    <DropShadowEffect Color="#3AA0AD" BlurRadius="10" ShadowDepth="2" Opacity="0.4"/>
                </Button.Effect>
            </Button>
        </Grid>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" BorderThickness="0">
            <ItemsControl x:Name="DepartmentList">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Left"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="300" Height="170" Margin="0,0,20,20" 
                Background="White" CornerRadius="12">
                            <Border.Effect>
                                <DropShadowEffect Color="#B0B0B0" BlurRadius="15" ShadowDepth="3" Opacity="0.2"/>
                            </Border.Effect>

                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Grid Grid.Row="0" Margin="15">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Width="60" Height="60" CornerRadius="10" Background="#E1F5FE" VerticalAlignment="Top">
                                        <TextBlock Text="🏢" 
                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                   FontSize="30"/>
                                    </Border>

                                    <StackPanel Grid.Column="1" Margin="15,0,0,0">
                                        <TextBlock Text="{Binding TenPb}" 
                                   FontSize="18" FontWeight="Bold" Foreground="#333" 
                                   TextTrimming="CharacterEllipsis"/>

                                        <TextBlock Text="{Binding MaPb}" 
                                   FontSize="14" Foreground="#3AA0AD" FontWeight="SemiBold" Margin="0,2"/>

                                        <Separator Background="#F0F0F0" Margin="0,8"/>

                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="📞 " FontSize="12"/>
                                            <TextBlock Text="{Binding SoDienThoai, TargetNullValue='(Chưa có SĐT)'}" 
                                       FontSize="12" Foreground="Gray"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Grid>

                                <Border x:Name="ActionPanel" Grid.Row="0" Grid.RowSpan="2" 
                        Background="#FFFFFF" CornerRadius="12" Opacity="0">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock Text="Thao tác" HorizontalAlignment="Center" Foreground="Gray" Margin="0,0,0,10"/>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Style="{StaticResource ActionButtonStyle}" Background="#66BB6A" 
                                    >
                                                <Path Data="M20.71,7.04C21.1,6.65 21.1,6 20.71,5.63L18.37,3.29C18,2.9 17.35,2.9 16.96,3.29L15.12,5.12L18.87,8.87M3,17.25V21H6.75L17.81,9.93L14.06,6.18L3,17.25Z" 
                                      Fill="White" Width="14" Height="14" Stretch="Uniform"/>
                                            </Button>

                                            <Button Style="{StaticResource ActionButtonStyle}" Background="#EF5350" 
                                   >
                                                <Path Data="M19,4H15.5L14.5,3H9.5L8.5,4H5V6H19M6,19A2,2 0 0,0 8,21H16A2,2 0 0,0 18,19V7H6V19Z" 
                                      Fill="White" Width="14" Height="14" Stretch="Uniform"/>
                                            </Button>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </Grid>
                            <Border.Triggers>
                                <EventTrigger RoutedEvent="MouseEnter">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ActionPanel" Storyboard.TargetProperty="Opacity" To="0.95" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                                <EventTrigger RoutedEvent="MouseLeave">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ActionPanel" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Border.Triggers>

                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>