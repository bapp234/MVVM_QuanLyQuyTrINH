<Window x:Class="MVVM_QuanLyQuyTrINH.Views.Windows.ThemDuAn"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Thêm Dự Án Mới" Height="600" Width="600"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Border CornerRadius="10" BorderBrush="#3AA0AD" BorderThickness="2" Padding="20" Background="White">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- ======= Tiêu đề ======= -->
            <TextBlock Text="🧩 Thêm Dự Án Mới"
                       FontSize="22"
                       FontWeight="Bold"
                       Foreground="#2C8A8A"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>

            <!-- ======= Form nhập ======= -->
            <StackPanel Grid.Row="1" Margin="0,10,0,10">

                <TextBlock Text="Tên dự án:" FontWeight="SemiBold"/>
                <TextBox x:Name="txtTenDuAn" Height="30" Margin="0,5,0,10"/>

                <TextBlock Text="Mô tả:" FontWeight="SemiBold"/>
                <TextBox x:Name="txtMoTa" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,5,0,10"/>

                <TextBlock Text="Ngày bắt đầu:" FontWeight="SemiBold"/>

                <DatePicker x:Name="dpNgayBatDau" Height="30" Margin="0,5,0,10"/>

                <TextBlock Text="Ngày kết thúc:" FontWeight="SemiBold"/>
                <DatePicker x:Name="dpNgayKetThuc" Height="30" Margin="0,5,0,10"/>

                <TextBlock Text="Trưởng nhóm phụ trách:" FontWeight="SemiBold"/>
                <ComboBox x:Name="cbTruongNhom" DisplayMemberPath="MaNvNavigation.HoTen" SelectedValuePath="MaNv" Height="30" Margin="0,5,0,10"/>

                <TextBlock Text="Trạng thái:" FontWeight="SemiBold"/>
                <ComboBox x:Name="cbTrangThai" Height="30" Margin="0,5,0,10" SelectedIndex="0">
                    <ComboBoxItem Content="Đang thực hiện"/>
                    <ComboBoxItem Content="Hoàn thành"/>
                    <ComboBoxItem Content="Tạm dừng"/>
                </ComboBox>
            </StackPanel>

            <!-- ======= Nút hành động ======= -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="💾 Lưu"
                        Width="100"
                        Height="35"
                        Background="SeaGreen"
                        Foreground="White"
                        FontWeight="Bold"
                        Margin="0,0,10,0"
                        Click="btnSave_Click"/>
                <Button Content="✖ Hủy"
                        Width="100"
                        Height="35"
                        Background="#E53935"
                        Foreground="White"
                        FontWeight="Bold"
                        Click="btnCancel_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
